name: Differential ShellCheck

on:
  push:
    branches:
      - master
      - 'rc/**'
    paths:
      - 'install.sh'
      - 'mudakop/files/usr/bin/**'
      - 'mudakop/files/usr/lib/**'
      - '.github/workflows/shellcheck.yml'
  pull_request:
    branches:
      - master
      - 'rc/**'
    paths:
      - 'install.sh'
      - 'mudakop/files/usr/bin/**'
      - 'mudakop/files/usr/lib/**'
      - '.github/workflows/shellcheck.yml'

permissions:
  contents: read

jobs:
  shellcheck:
    name: Differential ShellCheck
    runs-on: ubuntu-24.04
    
    permissions:
      contents: read
      security-events: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v5.0.0
        with:
          fetch-depth: 0
      
      - name: Differential ShellCheck
        uses: redhat-plumbers-in-action/differential-shellcheck@v5.5.5
        with:
          severity: error
          include-path: |
            mudakop/files/usr/bin/mudakop
            mudakop/files/usr/lib/**.sh
            install.sh
          token: ${{ secrets.GITHUB_TOKEN }}
